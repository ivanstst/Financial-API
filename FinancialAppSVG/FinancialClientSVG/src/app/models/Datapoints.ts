export class Datapoints {
    openTime: number;
    openPrice: number;
    constructor(openTime: number, openPrice: number) {
        this.openTime = openTime;
        this.openPrice = openPrice;
    }
}